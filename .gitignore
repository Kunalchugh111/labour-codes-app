__pycache__/
*.py[cod]
*$py.class
.streamlit/secrets.toml
# Per-model embedding indexes are experiment artifacts; only the canonical embeddings.npz is tracked
documents/processed/embeddings.*.npz
# Parsed-corpus snapshot; regenerated on any source change (see corpus._fingerprint)
documents/processed/corpus_cache.json
